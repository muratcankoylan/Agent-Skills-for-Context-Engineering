# 004 - Migration constraint retention

Held-out `context-compression` task focused on temporal retention. Critical constraints appear near the start; technical decisions, state, and risks appear much later.

The verifier checks sixteen task-specific anchors covering schema compatibility, artifact trail, early audit constraint, dual-write decision, state, data-loss risk, named approval gate, and next region. Literal heading names are not required.
