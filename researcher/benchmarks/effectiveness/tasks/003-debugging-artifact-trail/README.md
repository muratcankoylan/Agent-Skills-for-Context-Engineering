# 003 - Debugging artifact trail

Held-out `context-compression` task. It tests whether a bounded handoff preserves an exact failure/trace, root cause, modified and read-only files, rejected and selected recovery policy, test state, duplicate-order risk, named approval constraint, and continuation target.

The verifier uses thirteen task-specific anchors and does not require literal section names. This task must be analyzed separately from task 002 before any skill-effect claim.
